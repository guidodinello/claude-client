- [x] the claude client is getting quite big, should we fragment it into auth/project/docs services? and the main client works as an sdk? — done: `_transport.py` (auth/HTTP core) + `resources/` (`orgs`, `projects`, `docs`, `conversations`, `memory`), composed by `ClaudeClient`
- [x] also we could implement a cli tool to interact with the claude client, for example to upload/download files, sync projects, export project memory, etc.
- [x] adhere to coding standards and best practices. see claude md
- [x] add tests
- [x] we should have a method to export a project knowledge documents to a local folder as markdown files. also a sync one to keep local up to date with the web project. (maybe also a mirror set, local to web?)
- [x] we should be able to download the conversations (or just one by id?) of a project as markdown files, maybe one file per conversation. this should be part of the export project to markdown feature.
- [x] make `pull`/`pull_all` incremental over the network too: pull manifest keyed on remote uuid, skipping `get()` for conversations whose `updated_at` is unchanged — see [`docs/bugs/pull-re-fetches-unchanged-content.md`](docs/bugs/pull-re-fetches-unchanged-content.md). Docs still fetch unconditionally (list endpoint has no `updated_at`); fetch parallelization (below) is still open.
- [x] avoid n+1 on conversations requests / parallelize conversation fetches — orthogonal follow-up to the manifest work above (docs no longer need this: `docs.list()` already returns full content, verified live, so `DocsResource.pull` stopped calling `get()` per doc). Fixed: conversations needing a fetch (post-manifest-skip) are now submitted to a bounded `ThreadPoolExecutor` (`_MAX_CONVERSATION_FETCH_WORKERS = 5`) in `ConversationsResource._pull_conversations`, futures drained in submission order to preserve filename-collision tie-breaking; all dict/disk writes stay single-threaded.
- [x] `ProjectsResource.export_data` still calls `self._docs.get(project_id, meta["uuid"])` per doc — same redundant fetch as the old `docs.pull`, now provably unnecessary since `docs.list()` already returns full content. Fixed: `export_data` now uses `docs.list()`'s result directly, no per-doc `get()`.
- [x] consolidate `pull`/`pull_all` progress bars into one progress with per-project tasks (and identify the project in the label) — see [`docs/bugs/pull-progress-bars-accumulate.md`](docs/bugs/pull-progress-bars-accumulate.md). Fixed: `docs.pull`/`conversations.pull`/`ProjectsResource.pull` are now thin owners that open one `Progress` and delegate to internal `_pull_worker`/`_pull_into` variants taking a caller-supplied `progress` + `label`; `pull_all` opens a single `Progress` for the whole run with an overall per-project task plus per-project docs/conversations sub-tasks labeled by project name. Public signatures unchanged.
- [x] resource methods assume the wrong org on multi-org accounts for direct library callers — fixed: `Transport.org_id` now raises `AmbiguousOrgError` instead of guessing on a multi-org account with no pinned org; `client.for_project(project_id)` is the one-call fix — see [`docs/bugs/org-scoping-resource-methods.md`](docs/bugs/org-scoping-resource-methods.md)
- [x] `pull`/`pull_all` never prune local files deleted on the web (stale docs/conversations/project dirs accumulate) — fixed via the same manifest, `prune=True`/`--prune`, opt-in — see [`docs/bugs/pull-never-prunes-deleted-items.md`](docs/bugs/pull-never-prunes-deleted-items.md)
- [x] `_check_auth` reports a Cloudflare 403 challenge the same as a real invalid/expired token — see [`docs/bugs/cloudflare-403-reported-as-token-expired.md`](docs/bugs/cloudflare-403-reported-as-token-expired.md)
- [x] CLI command grouping/naming cleanup, part 1 (behavior fixes) — see [`docs/cli-consistency-review.md`](docs/cli-consistency-review.md): per-project org resolution, `project sync` now incremental, sync fetch-failure behavior aligned
- [x] CLI command grouping/naming cleanup, part 2 (API redesign: resource sub-clients, `pull`/`push` verbs, drop `projects` group) — see [`docs/cli-consistency-review.md`](docs/cli-consistency-review.md)